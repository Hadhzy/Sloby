import React from "react";


export enum SLOBY_SET_ERRORS {
  WRONGLY_DECLARED_VALUES = "If you are trying to add multiple values make sure that you add at least two, otherwise add just a single value"
}