import React from "react";


export enum SLOBY_SET_ERRORS {
  ARRAY_DEFINITION_RULE = "You have to define your set values within an Array, even if the values just a single element"
}

export enum STATUS {
  SUCCESS = "successfully implemented",
  ERROR = "An error occured"
}