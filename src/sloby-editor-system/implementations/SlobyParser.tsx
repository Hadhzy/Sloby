export default function SlobyParser() {
  let input = '<h5 style={color: "green", backgroundColor: "red"}>test</h5>'

//   Split the input into an array of strings by the first occurence of a space
  let splitInput = input.split(/(?<=\s)/);

  return (
    <></>
  )
}
